import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.floorroute.app',
  appName: '方寸识途',
  webDir: 'dist',
  backgroundColor: '#f5f7f2',
};

export default config;
